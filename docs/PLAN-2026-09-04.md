# PLAN — Aufbau-Editor (EditorAufbauV3), Stand 2026-09-04

**Zweck.** Der eine, vollstaendige Arbeitsplan. Er ersetzt `PLAN.md` (1080 Z.)
und fuehrt `docs/GRUNDLAGE.md` (1107 Z., Zweitanalyse) mit einer dritten,
unabhaengigen Analyse zusammen.

**An den Pruefer (Mensch oder KI).** Dieses Dokument ist selbsttragend: jeder
Befund nennt Datei und Zeile im Repo
`https://github.com/Maycetin1205/EditorAufbauV3` (Branch `master`, Stand
`cd94164`). Pruefe die Belege am Code, nicht dieses Dokument gegen sich
selbst. Wo "UNGEPRUEFT" steht, ist die Behauptung uebernommen und noch nicht
nachgesehen — behandle sie als Verdacht, nicht als Tatsache.

**Der Besitzer programmiert nicht.** Jeder Schritt braucht eine Klickprobe in
Klartext: was oeffnen, was tun, was zu sehen sein muss. Was nur in SoftEngine
sichtbar ist, kann nur er testen.

---

## 0. Herkunft der Befunde — wer hat was gefunden

| Quelle | Was |
|---|---|
| **A** = diese Analyse | F1, F2, F5, F6, F8, F9 |
| **B** = `docs/GRUNDLAGE.md` | F2, F3, F4, F7, F10, F11, F12, Slop-Liste, Schutzliste, Arbeitsregeln, Definition of Done |
| **beide unabhaengig** | F2 (Export-Leck), F7 (Kanban) |
| **widerlegt** | GRUNDLAGE B8 ("`master` hat einen Commit") — gemessen: `master` = **140 Commits**, `arena/01a06afe` = 139, `arena/01a06b85` = 141. Die Geschichte ist heil. Der dort geplante "SCHRITT 00 — Geschichte retten" **entfaellt**. |
| **offen** | Codex meldete am 2026-09-04 "zwei echte Verlustpfade bei Tabellenaenderungen und im lokalen Speicher", brach dann wegen Kontingent ab, ohne sie zu nennen. F5 und F6 sind der Versuch, sie selbst zu finden — ob es dieselben sind, ist unbelegt. |

---

## 1. BEFUNDE — kaputt

### F1 · Die Pruefung ist rot
`src/blocks/tabelle/zeilenAktivierung.test.ts:37` — `let curr: FakeElement | null = this`
loest `@typescript-eslint/no-this-alias` aus. `npm run check` endet mit Exit 1.
Eingefuehrt vom letzten Commit `cd94164` ("Schritt 19"), also gegen die eigene
Regel "Pruefbuendel vor jedem Commit" (`CLAUDE.md:37-40`).
`npm test` ist gruen: 40 Dateien, 339 Tests.
**Wirkung:** solange rot, sagt die Pruefung nichts mehr aus. **GEPRUEFT.**

### F2 · Geister-Felder im Export
`src/export/benutzteQuellen.ts:192` — `for (const source of sources)` laeuft
ueber die **gesamte** Quellen-Bibliothek (`sources = dataSourceStore.list`,
`exportMask.ts:213`). Dieselbe Datei macht es 120 Zeilen weiter oben richtig:
`:69` laeuft ueber `acc`, also nur ueber die im Baum gefundenen Quellen.

**Wirkung, praezisiert:** `baueSevariablen` (`src/export/sevariablen.ts:41`)
iteriert ueber `used` (baumgefiltert) und schlaegt Felder mit
`benutzteFelder.get(s.id)` nach. Eine nie platzierte Quelle kann daher **nicht
selbst** in den SEvariablen auftauchen — sie kann aber einer **benutzten**
Quelle zusaetzliche Feldcodes unterschieben. Das verstoesst gegen den
SE-Kontrakt "nur die BENUTZTEN Felder bestellen" (`CLAUDE.md:132-134`; ein `*`
erzeugte tausende Bild-Nachschlaege, 9,2 s).
**Umfang:** kleiner als zunaechst befuerchtet, aber falsch. **GEPRUEFT.**

### F3 · Spalten ab Nr. 17 werden stumm weggeworfen
`src/blocks/tabelle/spalten.ts:191` — `if (arr.length > SPALTEN_MAX) arr = arr.slice(0, SPALTEN_MAX)`
in `coerceSpalten`, ohne Meldung. Betrifft jede Lesung der Spaltenliste:
Maske laden, Migration, Attribut-Parsen (`tryCoerceSpalten:198`).

**Wirkung:** Ketten-Parameter werden ueber den PLATZ in der vollen Spaltenliste
eingefroren (`exportMask.ts:84-93`). Faellt eine Spalte weg, verrutschen die
Plaetze dahinter — die Laufzeit schreibt dann stumm falsche Werte ins ERP.
Genau davor warnt `spalten.ts:40-46` im eigenen Kommentar.
**Bricht:** `CLAUDE.md` Grundsatz 4 ("Nichts scheitert still").
**Gegenbild im selben Repo:** `spaltenBindung.ts:21-24` macht dieselbe Grenze
sichtbar (Plus-Knopf wird `disabled`). Zwei Disziplinen fuer eine Grenze.
**GEPRUEFT.**

### F4 · Gezogene Breite landet auf der falschen Spalte
Zwei Aufrufe derselben Funktion, einer unvollstaendig:

```
TabelleBlock.ts:594  spaltenSicht(spalten, this.hasAttribute('data-ff-editor'), this.wahlWeg())   vollstaendig
TabelleBlock.ts:354  spaltenSicht(this.spaltenListe(), this.hasAttribute('data-ff-editor'))       dritter Parameter fehlt
```

Signatur: `spalten.ts:58-65`, dritter Parameter
`wegDurchBediener: ReadonlySet<string> = new Set()`. Weil er einen Standardwert
hat, meldet der Compiler nichts.

**Wirkung:** `:350-358` uebersetzt gezogene Breiten vom gezeichneten auf den
vollen Platz. Die Uebersetzung behandelt `versteckt` (Bauer blendet aus), aber
nicht `wahlWeg` (Bediener blendet in der fertigen Maske aus). Der Zug schreibt
dann auf den Platz einer ausgeblendeten Spalte.
**Warum es niemand merkt:** im Editor ist `alleZeigen = true`, dann liefert
`spaltenSicht` die Identitaet (`:67`), und `wahlWeg()` ist ohnehin leer
(`TabelleBlock.ts:541-551`). Es braucht **vier** Bedingungen gleichzeitig:
fertige Maske + `spaltenwahl='ja'` + Bediener hat mindestens eine Spalte
weggenommen + zieht danach eine Breite. Editor, Tests und Sichtprobe sehen es
nie. **GEPRUEFT.**

### F5 · Beim Laden geht etwas verloren — ohne Sicherungskopie
`src/state/persistence.ts`. Es gibt einen fertigen Notfallkopie-Mechanismus
(`src/state/notfallkopie.ts`, `legeKopieAn`), und er wird auf zwei Wegen auch
benutzt: unlesbarer Stand (`backupUnreadableState`) und Stand aus neuerem
Editor (`meldeZukunftsStand`). Auf den **verlustbehafteten** Wegen nicht:

| Fall beim Laden | Meldung | Sicherungskopie |
|---|---|---|
| Bausteine verworfen (Typ existiert nicht mehr) | ja, `meldeVerworfeneTypen` | **nein** |
| Aktionsketten verworfen (nicht lesbar) | ja, `meldeVerloreneKetten` | **nein** |
| Bausteine absichtlich entfernt | ja, `meldeAbsichtlichEntfernte` | **nein** |

Der Code benennt die Folge selbst (`persistence.ts:57`):
*"Das muss man erfahren, denn der naechste Auto-Speicher schreibt den
gekuerzten Stand fest."* — und legt trotzdem keine Kopie an.
Verstaerkend: `Editor.ts:63` plant nach einer Schema-Wanderung sofort einen
Neu-Speicher (`resaveNeeded`). Spaetestens die naechste Aenderung ueberschreibt
das Original. **GEPRUEFT.**

### F6 · Spalte loeschen schaltet Ketten woanders ab — stumm
`src/state/spaltenAufraeumen.ts`, gerufen aus `Editor.ts:286`. Wird eine Spalte
gestrichen, setzt `ohneSpaltenZeiger` jeden Ketten-Parameter, der auf ihre
Kennung zeigte, auf `{ source: 'aus', value: '' }` — **im ganzen Baum**, also
auch auf Bausteinen, die der Bediener gerade gar nicht ansieht.

**Die Entscheidung ist richtig:** ein Zeiger ins Leere wuerde laut Kommentar
"kommentarlos einen Leerstring ins ERP schreiben".
**Das Problem ist die Stille.** Der Kommentar sagt "Bedienung am Ding statt
Warnung" — aber das Ding (eine Kette auf einem anderen Baustein) sieht man
nicht.
**Belegt:** `Editor.ts:266` ruft `pushHistory()` davor — Strg+Z holt es zurueck.
**Gefolgert, NICHT geklickt:** `persistState` schreibt nach 500 ms
(`SAVE_DEBOUNCE_MS`), die Historie lebt nur im Arbeitsspeicher. Nach einem
Neuladen ist der Verlust endgueltig und war nie sichtbar.

### F7 · Kanban mit genau einer Spalte ist nicht greifbar
Der Editor hat **zwei** Zieh-Systeme in `src/editor/canvas/CanvasNode.tsx`:

- Raster-Bausteine: `onPointerDown` -> `rasterMove.ts ziehePosition` (`:139`)
- Fluss-Kinder: natives HTML5 `draggable` (`:152-160`)

`KanbanSpalteBlock.ts:37` hat `lockedWidth: 'fill'`. Bei **einer** Spalte
bedeckt sie jeden Pixel des Bretts. Der native HTML5-Drag der Spalte
unterbindet die `pointermove`-Ereignisse, auf die `ziehePosition` wartet
(es kommt `pointercancel`) — der Board-Zug wird nie aktiv (`aktiv` bleibt
`false`, `rasterMove.ts:66-74`). Bei drei Spalten rettet nur die
`gap: var(--se-gap-lg)` zwischen ihnen: dort liegt `.board` frei.
**Betrifft nicht nur Kanban**, sondern jeden Container, dessen Kinder die
volle Flaeche fuellen. **GEPRUEFT (aus dem Code, nicht im Browser geklickt).**

### F8 · 97 % jeder exportierten Maske ist Bausatz
`src/blocks/register.ts` laedt **alle 15 Bausteine** bedingungslos.
Gemessen am Referenzabzug im Repo:

```
src/export/referenz/referenz.html        242.776 Bytes
src/export/generated/ff-runtime.js       235.583 Bytes  = 97,0 %
--------------------------------------------------------
eigentliche Maske                          7.193 Bytes  =  3,0 %
```

`PLAN.md` kennt das Problem und hat dafuer die Schritte 11-16b ("nur was drauf
liegt, kommt in die Maske"). Erledigt sind 11 und 12; **13, 14, 15, 16a, 16b
stehen aus**, am 2026-09-03 zugunsten der Tabellen-Bedienung zurueckgestellt.
**GEPRUEFT (Dateigroessen gemessen).**

### F9 · Alles zeichnet bei jeder Kleinigkeit neu
`src/state/useEditor.ts:12` — `useSyncExternalStore(abonniere, () => editor.version)`
ohne Selektor. Jede Komponente, die `useEditor()` ruft, rendert bei **jeder**
Zustandsaenderung neu, auch bei einem Tastendruck im Inspector.
**Wirkung:** Hakeln bei grossen Masken. Kein Fehler, eine Bremse. **GEPRUEFT.**

### F10 · Muster-Karte bekommt einen Rahmen, aber kein Werkzeug — UNGEPRUEFT
Aus GRUNDLAGE B5. Verwandt und von mir geprueft: `BlockHost.tsx:110-112`
zeichnet fuer **jeden** Baustein denselben rechteckigen Rahmen
(`outline: 2px solid hsl(var(--wb-auswahl))`, `outlineOffset: 1`), auch fuer
Karten mit runden Ecken. Die Aussage ueber die fehlende Werkzeugleiste ist
nicht nachgesehen.

### F11 · Horcher werden nicht abgemeldet — UNGEPRUEFT
Aus GRUNDLAGE B6: `datenAnschluss.disconnect` entfernt keine Listener.
Laut GRUNDLAGE praktisch folgenlos, als Regel falsch.

### F12 · Export ist unrein — UNGEPRUEFT
Aus GRUNDLAGE B7: gefaehrdet den byte-bewachten Referenzabzug (Gefahr:
zufaellig roter Test ohne echte Aenderung).

### F13 · Slop-Kleinteile — UNGEPRUEFT, aus GRUNDLAGE Abschnitt 6

| Was | Wo |
|---|---|
| Doppelter Kommentarblock, beide mit kaputtem Deutsch | `spaltenBearbeiten.ts:64-67` **und** `:69-72` |
| Tote Funktion `entferneSpalte` | `spaltenBearbeiten.ts:45-53` |
| Tote Funktion `verschiebeSpalteAn` | `spaltenBearbeiten.ts:87-96` |
| Zerbrochene Zeilenformatierung | `TabelleBlock.ts:643-648` |
| Ueberschreib-Warnung statt Fehler | `blockRegistry.ts:6-8` |
| 3 rohe `<input>` neben 103 Werkbank-Importen | `BildControl.tsx:54`, `Toolbar.tsx:222`, `DatenquellenBereich.tsx:113` |
| Doku-Drift | `CLAUDE.md:150-151` behauptet Datencenter-Reiter; Code: `Inspector.tsx:208` |

### F14 · Doppelte Anmeldung / Auslaufmodell — UNGEPRUEFT, GRUNDLAGE L6
`BasicBlock.ts:117-122` `defineAndRegister` sollte mit "Schritt 12b" wegfallen.
Bausteine rufen es (`KanbanBlock.ts:106`, `CardBlock.ts:159`,
`TabelleBlock.ts:722`) **und** es gibt je eine `editorAngaben.ts`.
Vermutete Folge: `blockRegistry.ts:6-8` `console.warn("... wird ueberschrieben")`.
**Nachweis: Editor starten, Konsole ansehen.**

---

## 2. BEFUNDE — fehlt (Wuensche des Nutzers)

### W1 · Maximale Spaltenzahl je Baustein einstellbar
Heute nur die globale Konstante `spalten.ts:98` `SPALTEN_MAX = 16`.
`BlockDefinition.ts:152-215` hat 33 Felder — `maxSpalten` ist keins davon.
`tabelleEigenschaften.ts:5-52` hat 7 Ja/Nein-Schalter, keine Obergrenze.
**Entscheidung des Nutzers noetig:** nein. Standard bleibt 16.

### W2 · Dynamische Spaltenbreite
Entspricht `PLAN.md` Abschnitt 6, Punkt 3 ("Anzeigelaenge je Feld im
Datencenter; Spaltenbreite als Anteil daraus"). **Steht dort seit 2026-09-03
unter der Ueberschrift "Wartet auf Entscheidung — NICHT bauen".**

**WARNUNG, aus `PLAN.md`:** *"`tabelle/spalten.ts` haelt fest, dass ZWEI
Anlaeufe mit gerechneten Breiten gescheitert sind (rechts blieb Flaeche leer,
Nutzer-Befund 2026-08-31)."* Daraus abgeleitete Regel, die gelten muss:
**die Anzeigelaenge darf nur die VORGABE setzen, wo keine Breite gezogen wurde
— nie eine gezogene Breite ueberschreiben.**
`DataSourceField` ist heute `{ code, label }`; eine dritte, optionale Angabe
passt hinein, darf aber **nie** in die SEvariablen wandern (die pos_len-Liste
haengt am `code`, SE-Kontrakt).

### W3 · Spalten-Umbruch auf eine zweite Zeile
`tabelleAnsicht.ts:127-129` rechnet **eine** `gridTemplateColumns`.
`tabelleKoerper.ts:178` zeichnet **eine** Kopfzeile, jede Zelle `grid-row: 1`
(`:190`). Lineal (`:148-160`) und Erfassungszeile haengen am selben `cols`.
**Beruehrt:** `tabelleAnsicht`, `tabelleKoerper`, `tabelleStil.ts` (479 Z.),
`spaltenBreite.breitenGriffe` (Index-Mathematik) **und das exportierte HTML**.
**Erste Falle (GRUNDLAGE):** Kopfzelle *und* Greifstreifen muessen `grid-row`
beide ausdruecklich nennen, sonst rutschen Zellen unbeabsichtigt in eine zweite
Reihe. Das ist schon einmal versehentlich passiert.
**ENTSCHEIDUNG DES NUTZERS NOETIG.** Bereits entschieden am 2026-09-04:
**kein Scrollen.** Offen bleiben: zweite Kopfzeile / zweite Datensatzzeile /
etwas anderes. Der Nutzer entscheidet **am Bild**, nicht an einer Beschreibung.

### W4 · Pfad-Auswahl
`Editor.ts:228` `selectBlock(id: string | null)` kennt **eine** Id. Kein
`[board, spalte, karte]`, kein "eine Ebene hoch". `BlockHost.tsx:112` zeichnet
genau einen Rahmen.
**Noetig fuer:** F7 (Board erreichbar machen ohne Export-Aenderung) und F10.

### W5 · Kanban: Obergrenze, Scrollen, Mindestbreite
`KanbanBlock.ts:41-47` kein `maxSpalten`. `:79-86` `.board` hat **kein**
`overflow-x`. `KanbanSpalteBlock.ts:74-78` hat `min-height:100%`, aber **kein**
`min-width`.
**Achtung:** das `.board`-CSS steht in `KanbanBlock.ts:74-89` und **landet im
Export** -> Referenzabzug Teil B + Browser-Schranke.
**ENTSCHEIDUNG DES NUTZERS NOETIG:** welcher Standard fuer `maxSpalten` (1-8)?
Nicht raten.

---

## 3. SCHUTZLISTE — NICHT ANFASSEN

Uebernommen aus `docs/GRUNDLAGE.md` Abschnitt 7. Das ist Schutz, nicht Lob.
Wer hier "aufraeumt", baut Fertiges ab.

Registry + Faehigkeiten (`BlockDefinition.ts:152-215`, `treeQuery.ts:87-196`,
**eine** einzige `if typ`-Stelle in ganz `src/`) · `spaltenSicht` + `plaetze`
(`spalten.ts:40-82`, 5 Tests) · zwei Zahlen-Leser `zahlStreng`/`alsZahl`
(beide getestet) · Luecken-Rechnung (`rechnung.ts:103-140`) · Rechnung im
Inspector · Inspector-Reihenfolge und -Trennung (`Inspector.tsx:129-216`) ·
Editor-Chrome nur im Host (`BlockHost.tsx`) · Editor/Maske-Trennung (21
`data-ff-editor`-Wachen, `datenAnschluss.ts:30`) · gezogene Breite als Anteil
(`fr`-Raster, `spaltenBreite.ts:23-111`) · Spalten-Kennung als dauerhafter
Ausweis · Enter/Tab-Fluss der Erfassung · Laufzeit-Vertraege · Kanban-DnD in
der Maske (natives HTML5, `seRuntime.ts:206-258`) · Karte =
Baustein/Vorlage/Klon · Export-Wurzel = Baum · Werkbank (20 Teile, 103
Importe) · Undo/Redo · State-Faecher · Pruefnetz (40 Testdateien,
byte-bewachter Referenzabzug, Sichtprobe) · keine Demo-Daten.

**Nicht geprueft — NICHT als "richtig" uebernehmen:** `src/softengine/`
(11 Dateien, `relations.ts` 409 Z.), `docs/chef-maske/`,
`RECHNUNG-BELEGERFASSUNG.md`, `tabelleStil.ts` (479 Z.), `erfassungsLauf.ts`
ausser `:100-215`, `zeilenBearbeitung.ts`, `erfassungsAnschluss.ts`,
`ansichtsStand.ts`, `FormFeldBlock.ts`, `ladeKette.ts`, `migrationenRoh.ts`.

**Solange `src/softengine/` nicht gegen die SE-Kontrakte in `CLAUDE.md`
geprueft ist, werden die Kontrakte NICHT angefasst.**

---

## 4. ARBEITSREGELN

1. **`master` ist der einzige Branch.** Kein Arbeits-Branch, kein force-push.
2. **Vor jedem Schreiben lesen.** Wer eine Datei anfasst, die er nicht
   geoeffnet hat, wird gestoppt.
3. **Doku ist Zeuge, nicht Beweis.** `CLAUDE.md` sagt das selbst (`:3-7`),
   `CLAUDE.md:150-151` ist der belegte Gegenbeweis. Nutzer-Bestaetigung
   2026-09-04: die dem Nutzer zugeschriebenen "Entscheidungen" in `CLAUDE.md`
   und `PLAN.md` sind **nicht** von ihm. Sie sind unverbindlich.
4. **Kein Wert wird "verbessert", ohne die Begruendung im Code zu lesen.**
   `SPALTEN_MAX = 16` (`spalten.ts:91-97`), `ZEILEN_HOEHE = 28`,
   `SPALTEN_MIN_BREITE = 40`, `13.5px`, `240px`, `400px`, `2px`.
5. **Nichts wird als "neu" gebaut, bevor `grep` + `ls` geprueft haben, ob es
   existiert.**
6. **Browser-Schranke.** Kein CSS im Export, das Chromium < 87 nicht kann.
   Kein `inset: 0`. Beleg: `spaltenBreite.ts:113-121` (Nutzer-Befund
   2026-08-31: eine Lage hatte im SoftEngine-Browser keine Groesse).
7. **Export-Neutralitaet zuerst.** Aenderungen an `src/blocks/**/styles`,
   `src/design/`, `tabelleStil.ts`, `kartenStil.ts`, `KanbanBlock.ts` aendern
   das Export-HTML -> Referenzabzug **Teil B** mit Diff-Erklaerung.
   Aenderungen nur in `src/editor/`, `src/state/`, `src/ui/` muessen
   **Teil A gruen lassen**.
8. **Ein Chat = ein Schritt.** Was groesser ist, wird geteilt. Ein
   abgebrochener Kontext mitten im Umbau ist der teuerste Fehler.
9. **Erst der rote Test, dann der Fix.** Bei F2, F3, F4 muss der Test **rot**
   sein, bevor eine Zeile geaendert wird. Sonst ist nicht bewiesen, dass der
   Fehler existierte.
10. **Raten verboten.** Unklares wird als OFFEN notiert und gefragt.
11. **Historische Nummern nicht neu belegen.** Vergeben sind `S1-S6`,
    `P1-P6`, `T1-T7`, `Etappe 0-4`, `Schritte 1-20`. **Neue Schritte ab 21.**

---

## 5. DEFINITION OF DONE — fuer JEDEN Schritt

```
 1. git fetch
 2. npm run check           -> Exit 0     (tsc -b && eslint src)
 3. npm run build:runtime   -> Exit 0     * leicht vergessen
 4. npm test                -> gruen      Zahl darf gleich bleiben
 5. Referenzabzug
    Teil A  ohne REFERENZ_ERNEUERN   -> muss gruen BLEIBEN (export-neutrale Schritte)
    Teil B  mit REFERENZ_ERNEUERN=1  -> Diff erklaeren
    Grenze: alles zwischen window.FF_RELATIONS und </script> ist Buendel,
            der Rest ist Maske.
 6. Sichtprobe: node tools/sichtprobe.cjs standard (Dev-Server Port 5300)
    -> neun Bilder nach sichtprobe/, JEDES EINZELN ansehen
 7. Klickprobe bestanden, Klickanleitung im Bericht
 8. Browser-Schranke geprueft: kein neues CSS < Chromium 87
 9. 1 Commit, Klartext "Schritt NR - Ziel", Dateien NAMENTLICH gestagt
    (kein git add -A), kein force-push, nur master
10. Dieses Dokument fortgeschrieben: Schritt auf erledigt, Befund gestrichen
11. Bericht in Klartext: was geht - was NICHT geprueft wurde - was offen ist.
    Keine Technik-Reviews, kein Cheerleading.
12. Zweitmeinung (Codex) hat gegengelesen, bevor "fertig" gemeldet wird.
    Faellt sie aus (Kontingent), wird das im Bericht GESAGT, nicht verschwiegen.
```

---

## 6. DIE SCHRITTE

### VORLAUF · V1 — Ein Dokument statt vier
**Kein Code.** `docs/GRUNDLAGE.md` und dieses Dokument werden auf `master`
zusammengefuehrt. `PLAN.md` wird auf das eingedampft, was belegt ist (die
gemessenen Zahlen bleiben, die erfundenen Nutzer-Zitate fliegen). Aus
`CLAUDE.md` fliegt jede dem Nutzer zugeschriebene "Entscheidung", die er nicht
bestaetigt hat. Die SE-Kontrakte (`CLAUDE.md:101-144`) bleiben unangetastet.
**Klickprobe:** der Nutzer liest. Jeder Satz, den er nie gesagt hat, fliegt.
**Risiko:** keines (kein Code).

---

### PHASE 0 · Fundament

#### SCHRITT 21 — Die Pruefung wieder gruen machen · F1
**Ausgangslage:** `npm run check` Exit 1.
**Aenderung:** `zeilenAktivierung.test.ts:37` — `this` nicht mehr in eine
lokale Variable aliasen; die Schleife so umformen, dass die Regel nicht
ausloest. **Nur die Testdatei.**
**Test:** `npm run check` ist der Test. Vorher Exit 1, nachher Exit 0.
**Klickprobe:** keine — am Editor aendert sich nichts.
**Export:** neutral. **Risiko:** minimal. Ist `npm test` danach nicht mehr
gruen, war die Umformung falsch — dann zurueck.
**Warum zuerst:** ohne gruene Pruefung sagt kein spaeterer Schritt etwas aus.

#### SCHRITT 22 — Geister-Felder raus aus dem Export · F2 (+ F12 pruefen)
**Ausgangslage:** `benutzteQuellen.ts:192` iteriert `sources`.
**Zuerst der rote Test:** eine Maske mit zwei Quellen — Quelle A liegt auf dem
Canvas, Quelle B nur in der Bibliothek, und B zieht einen Parameter aus A.
Erwartung: `benutzteFelderJeQuelle` darf fuer A **nicht** das Feld enthalten,
das nur B braucht. Der Test muss **rot** sein.
**Aenderung:** die Schleife auf die baumgefilterte Menge umstellen. Achtung,
zwei dokumentierte Faelle duerfen nicht brechen: (a) Parameter holender
Quellen, (b) Nachschlage-Hilfsquellen (`:113-126`).
**Nebenauftrag:** dabei F12 ("Export unrein") pruefen — erweist er sich als
haltlos, wird er gestrichen, nicht stillschweigend mitgeschleppt.
**Klickprobe:** Datencenter -> Quelle anlegen, **nicht** aufs Canvas legen ->
exportieren -> `index.basis.SEvariablen.json` vorher/nachher vergleichen. Es
darf kein Feld dazugekommen sein.
**Export:** Teil A muss **gruen bleiben** (die Referenzmaske hat keine
Geister-Quelle; aendert sich ihr Export, ist der Fix zu breit).

---

### PHASE 1 · Nichts kann mehr unbemerkt verschwinden

#### SCHRITT 23 — Sicherungskopie, bevor beim Laden etwas wegfaellt · F5
**Aenderung:** in `loadFromStorage` auf den drei verlustbehafteten Wegen
`legeKopieAn(STORAGE_KEY, raw)` rufen, **bevor** der gekuerzte Stand
zurueckgegeben wird, und den Kopie-Hinweis in die bestehende Meldung
aufnehmen (`kopieSatz`, wie in `meldeZukunftsStand`).
**Test:** ein Stand mit einem unbekannten Bausteintyp -> nach dem Laden muss
der Notfallkopie-Schluessel existieren und den urspruenglichen Text enthalten.
**Klickprobe:** schwer selbst auszuloesen; der Test wird vorgefuehrt.
**Export:** neutral. **Risiko:** Speicherplatz — eine zusaetzliche Kopie im
Browser-Speicher. Bei Ueberlauf greift `meldeSpeicherPanne`.

#### SCHRITT 24 — Spalten-Obergrenze einstellbar + Ueberzaehlige melden · W1 + F3
**Zwei Dinge, ein Schritt, weil sie dieselbe Stelle anfassen.**

**a) W1:** ein optionales Feld `maxSpalten` in `BlockDefinition` +
`BlockComponent` + `beschreibe()` (`BasicBlock.ts:23-66`) + ein Control im
Inspector. **Standard bleibt 16** — kein bestehendes Verhalten aendert sich.
Der Plus-Knopf haengt bereits an einer Grenze (`spaltenBindung.ts:21-24`,
`AuswahlLeiste.tsx:81,106`) und liest kuenftig diese.

**b) F3:** `coerceSpalten` schneidet nicht mehr stumm. **Entscheidung:**
melden **und** alle Plaetze erhalten (nicht schneiden) — weil das Schneiden
laut `spalten.ts:40-46` die Ketten-Parameter verrutschen laesst. Meldung ueber
`meldungen.melde` in Klartext.

**Zuerst der rote Test:** 17 Spalten laden -> heute kommen 16 zurueck, ohne
Meldung. Test erwartet 17 und eine Meldung.
**Klickprobe:** Tabelle waehlen -> Inspector -> Obergrenze auf 5 -> Plus-Knopf
wird grau bei 5 Spalten. Und: eine Maske mit 17 Spalten laden -> Meldung im
Fehlerbalken.
**Export:** neutral. **Risiko:** `SPALTEN_MAX` wird an mehreren Stellen
gelesen — alle finden, keine vergessen.

#### SCHRITT 25 — Sagen, wenn eine geloeschte Spalte Ketten abschaltet · F6
**Aenderung:** `ohneSpaltenZeiger` gibt zurueck, **wie viele** Parameter auf
`aus` gesetzt wurden und auf welchen Bausteinen; `Editor.ts:286` meldet das
ueber `meldungen.melde` in Klartext ("Spalte X geloescht — N Ketten-Parameter
auf 2 Bausteinen wurden abgeschaltet").
**Test:** Baum mit einer Kette auf Spalte 2, Spalte 2 loeschen -> Rueckgabe
muss 1 melden.
**Klickprobe:** Kette auf eine Spalte legen -> Spalte loeschen -> Meldung muss
erscheinen -> Strg+Z -> Kette ist wieder da.
**Export:** neutral.

#### SCHRITT 26 — Gezogene Breite landet auf der richtigen Spalte · F4
**Zuerst der rote Test:** 5 Spalten, `wahlWeg` enthaelt Spalte 2,
`alleZeigen = false`. Erwartung: `sicht.plaetze === [0,2,3,4]`. Heute liefert
der Pfad in `vollerPlatz` `[0,1,2,3]`. Der Test muss **rot** sein.
**Aenderung:** `TabelleBlock.ts:354` — `this.wahlWeg()` als dritten Parameter
nachreichen. **Eine Zeile.**
**Klickprobe (nur der Nutzer, nur in SoftEngine):** `spaltenwahl` einschalten
-> exportieren -> in SoftEngine eine Spalte wegnehmen -> an einer Spaltenkante
ziehen -> die Breite muss sich **dort** aendern, wo gezogen wurde.
**Export:** neutral (Editor-Verhalten unveraendert, da dort `alleZeigen`).
**Risiko:** gering. Aber die Wirkung ist nur in SoftEngine sichtbar — hier
endet, was der Agent pruefen kann.

---

### PHASE 2 · Die Tabelle des Nutzers

#### SCHRITT 27 — Dynamische Spaltenbreite · W2
**Erst Vorarbeit, dann Code.** Vor der ersten Zeile wird schriftlich
festgehalten: warum sind die zwei frueheren Anlaeufe gescheitert
(`spalten.ts`, Nutzer-Befund 2026-08-31, "rechts blieb Flaeche leer"), und was
wird diesmal anders. Erst wenn das steht und der Nutzer es gelesen hat, wird
gebaut.
**Bindende Regel:** die Anzeigelaenge setzt nur die **Vorgabe**, wo keine
Breite gezogen wurde. Eine vom Nutzer gezogene Breite wird **nie**
ueberschrieben.
**Kontrakt:** die neue, optionale Angabe an `DataSourceField` darf **nie** in
die SEvariablen wandern.
**Klickprobe:** Tabelle mit unterschiedlich langen Feldern -> Spalten sind
sinnvoll breit -> dann eine Breite ziehen -> neu laden -> die gezogene Breite
steht noch.
**Export:** Teil B, Diff erklaeren. **Risiko: hoch.** Zweimal gescheitert.

#### SCHRITT 28 — Spalten-Umbruch · W3
**BLOCKIERT.** Wird nicht begonnen, bevor der Nutzer **am Bild** entschieden
hat. Bereits entschieden: **kein Scrollen** (Nutzer, 2026-09-04).
**Vorstufe:** dem Nutzer werden die Bauarten als Bild gezeigt, mit echten
Belegerfassungs-Spalten, nicht als Beschreibung.
**Export:** Teil B, Diff erklaeren. **Risiko: hoch** (Gitter-Mathematik,
Greifstreifen-Indizes und Erfassungszeile haengen alle am selben `cols`).

---

### PHASE 3 · Bedienung

#### SCHRITT 29 — Jeder Container ist erreichbar · W4 + F7
**Aenderung:** Auswahl bekommt einen Pfad statt einer Id. `Escape` waehlt den
Elternteil. Damit wird das Kanban-Board erreichbar, **ohne** das Export-HTML
zu aendern — das ist der Grund, diesen Weg zu waehlen und nicht eine
Board-Titelzeile oder `.board`-Padding (beides wuerde die fertige Maske
veraendern).
**Klickprobe:** Kanban mit **einer** Spalte -> Spalte anklicken -> `Escape` ->
Board ist gewaehlt -> Board laesst sich verschieben.
**Export:** neutral. **Risiko:** mittel, fasst die Auswahl im ganzen Editor an.

#### SCHRITT 30 — Muster-Karte und Auswahlrahmen · F10
**Zuerst pruefen, ob F10 stimmt.** Wenn ja: der Rahmen wird nicht mehr
einheitlich als Rechteck gezeichnet — der Baustein sagt, wie er markiert wird
(Registry-Eintrag, **kein** `if typ === 'kanban'`, Grundsatz 2).
**Klickprobe:** Kanban-Karte anklicken -> Markierung passt zur Form der Karte.
**Export:** neutral, solange nur `src/editor/` angefasst wird.

#### SCHRITT 31 — Kanban: Obergrenze, Scrollen, Mindestbreite · W5
**BLOCKIERT** bis der Nutzer den Standard fuer `maxSpalten` (1-8) nennt.
**Aenderung:** `maxSpalten` am Kanban, `overflow-x: auto` am `.board` ab der
Grenze, `min-width: 220px` je Spalte.
**Achtung:** `.board`-CSS steht in `KanbanBlock.ts:74-89` und **landet im
Export** -> Teil B + Browser-Schranke pruefen (`overflow-x` und `min-width`
sind beide aelter als Chromium 87, also unbedenklich).

---

### PHASE 4 · Der grosse Brocken

#### SCHRITT 32 — Nur benutzte Bausteine in die Maske · F8
Die 97 %. Entspricht `PLAN.md` Schritte 13, 14, 15, 16a, 16b — dort steht die
Vorarbeit schon. **Mehrere Teilschritte, jeder einzeln pruefbar**, keiner
groesser als ein Chat.
**Klickprobe:** Maske nur mit einer Tabelle exportieren -> Datei deutlich
kleiner -> **und in SoftEngine laufen wie vorher.** Das ist die eigentliche
Pruefung, und nur der Nutzer kann sie machen.
**Export:** Teil B, grosser Diff. **Risiko: hoch.** Kommt deshalb spaet.

---

### PHASE 5 · Hygiene

#### SCHRITT 33 — Toter Code, doppelte Anmeldung, Doku-Drift · F11 + F13 + F14
Jeder Punkt **erst pruefen, dann anfassen**. Was sich als haltlos erweist,
wird gestrichen und im Bericht genannt.
**Export:** neutral.

#### SCHRITT 34 — Render-Bremse · F9
`useEditor` bekommt Selektoren, damit nicht jede Komponente bei jeder
Aenderung neu zeichnet.
**Klickprobe:** grosse Maske, im Inspector tippen -> darf nicht haken.
**Export:** neutral. **Risiko:** mittel — falsche Selektoren zeigen veraltete
Werte an.

---

## 7. OFFEN — bevor der jeweilige Schritt beginnt

| # | Frage | Wer entscheidet | Blockiert |
|---|---|---|---|
| O1 | Spalten-Umbruch: welche Bauart? (kein Scrollen steht fest) | **Nutzer, am Bild** | Schritt 28 |
| O2 | Kanban `maxSpalten`: welcher Standard (1-8)? | **Nutzer** | Schritt 31 |
| O3 | Die zwei Verlustpfade, die Codex fand | Codex, ab Kontingent | — |
| O4 | Sind F10, F11, F12, F13, F14 echt? | Agent, vor dem Anfassen | 30, 33 |
| O5 | `src/softengine/` gegen die SE-Kontrakte pruefen? | Nutzer (Aufwand) | Kontrakte anfassen |

---

## 8. WAS NICHT PASSIERT

- Kein Neubau. Nirgends. Die SE-Kontrakte in `CLAUDE.md` stammen aus echten
  Tests des Nutzers ueber Wochen; ein Neubau riskiert jeden einzelnen davon.
- Keine Umbenennungen und kein "Aufraeumen bei der Gelegenheit".
- Kein `if typ === '<baustein>'` im Editor- oder Export-Kern.
- Kein CSS im Export, das Chromium < 87 nicht kann.
- Die Rechnung (`core/data/rechnung.ts`) bleibt.
- Keine Beispieldaten im Editor.
- Nichts wird gebaut, wo der Nutzer entscheiden muss und noch nicht
  entschieden hat.
